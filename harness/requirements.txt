# Quest Benchmark Harness dependencies
# Frozen at methodology lock date: 2026-06-09

# Core
python-dotenv>=1.0.0
requests>=2.31.0

# Data / ML
numpy>=1.26.0
scikit-learn>=1.4.0
sentence-transformers>=2.5.0  # for naive-RAG baseline embedding
chromadb>=0.5.0  # for tools that need vector store

# LLM transports
openai>=1.30.0  # OpenAI / OpenRouter compatible
anthropic>=0.28.0  # Anthropic API
ollama>=0.2.0  # Ollama client

# Database (for adapters that need it)
psycopg2-binary>=2.9.0  # PostgreSQL / pgvector
redis>=5.0.0

# Markdown / parsing (for Obsidian baseline)
markdown>=3.6.0

# Dev / testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
